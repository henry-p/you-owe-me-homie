<%= current_user.first_name %>

<%= link_to "Create dwelling", new_household_path %>

<% @households.each do |household| %>

  <%= link_to "#{household.name}", household_path(household) %>

<% end %>
